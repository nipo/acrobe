[build-system]
requires = ["setuptools>=68.0", "Cython>=3.0"]
build-backend = "setuptools.build_meta"

[project]
name = "acrobe"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["libusb1", "asyncclick", "ausb", "pyyaml", "aiohttp", "cbor2", "hid", "watchdog"]

[project.scripts]
acrobe = "acrobe.cli.console:main"

[project.optional-dependencies]
test = ["pytest", "pytest-asyncio"]

[tool.setuptools.packages.find]
include = ["acrobe*"]

# Adapters and components load their bitstreams from a "fw" directory
# next to their Python; without this the wheel silently ships none of
# them and those adapters fail at runtime.
[tool.setuptools.package-data]
"*" = ["**/fw/*.gz"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
